# Ignore everything
/*

# Except these files & folders
!/src
!/public
!/.github
!/worker
!tsconfig.json
!astro.config.ts
!wrangler.jsonc
!package.json
!.prettierrc
!eslint.config.js
!README.md
!.vscode

# Ignore MDX files
*.mdx
# 内容目录不归 prettier 管。
#
# 文章主要在 /admin/ 后台写，而后台是浏览器直接调 GitHub API 往 main 提交
# （public/admin/config.yml 里 backend.branch: main），不经过任何本地 git ——
# 装 pre-commit 钩子拦不住它。
#
# 而后台序列化 markdown 的口味和 prettier 不一样：分隔线它输出 ***，
# prettier 要 ---。这不是谁写错了，是两个工具的默认值对不上，所以
# **每次**在后台改一篇带分隔线的文章，format:check 都会红一次。
# 9bfabd2 就是这么来的，把 CI 卡了三周，期间 build 一次都没跑。
#
# 摘出去损失的只是散文的排版一致性，不影响构建产物。内容的真正守门员是
# check:posts（frontmatter、缩略图）和 astro build，那两道还在。
# 代码部分照旧全归 prettier 管。
#
# 想手动格式化某篇时：npx prettier --write --ignore-path /dev/null <文件>
/src/content

# KaTeX 的样式表不归 prettier 管。
#
# 这个目录整个是 scripts/sync-katex.mjs 从 node_modules 原样拷过来的，
# 上游发的就是压缩过的一行 CSS。prettier 会把它展开成 1156 行 ——
# 于是 public/ 里多出一个「派生且和上游不一致」的文件，正是那个脚本
# 开头明说要避免的情况；而且展开后 28,091 B，比上游的 24,727 B 多 3.4 KB，
# 每个打开带公式文章的读者都要多下这一截。
#
# 实测过：升级到 0.18.4 之后 format:check 会红在这个文件上（之前那份
# 是被 prettier 展开过的，所以一直是绿的）。要改的是这条规则，不是那个文件。
/public/katex
